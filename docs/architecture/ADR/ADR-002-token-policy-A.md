# ADR-002: Token Policy A (토큰 없으면 raw 유지)

## 결정
토큰 매핑 실패 시 근사 매핑하지 않고 raw 값을 유지한다.

## 근거
정확도/예측가능성 우선. 잘못된 자동 근사는 장기 유지보수 비용을 높임.

## 대안
클러스터링/근사 매핑(기각: 오탐/불투명성 증가).

## 결과
토큰화 부채가 리포트로 드러나고, 후속 정리 우선순위가 명확해짐.
